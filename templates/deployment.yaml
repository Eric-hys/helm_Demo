apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eric-demo.fullname" . }}
  labels:
    app: {{ include "eric-demo.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "eric-demo.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "eric-demo.name" . }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80

